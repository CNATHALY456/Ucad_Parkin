import 'package:flutter/material.dart';

class RegistroSalida extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Registro Salida")),
      body: Center(child: Text("Pantalla Salida")),
    );
  }
}
