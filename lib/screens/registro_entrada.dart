import 'package:flutter/material.dart';

class RegistroEntrada extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Registro Entrada")),
      body: Center(child: Text("Pantalla Entrada")),
    );
  }
}
